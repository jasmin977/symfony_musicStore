{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}
{%  block sliders %}{% endblock %}

{% block body %}

<div class="featured pad" id="featuredalbum">
	<div class="container">
    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ genre.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                 <td><img src ="{{ asset('uploads/images/' ~ genre.image) }}"
   alt =""  width="200" height="200"></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('genre_index') }}"> back to list</a>

    <a href="{{ path('genre_edit', {'id': genre.id}) }}"> edit</a>

    {{ include('genre/_delete_form.html.twig') }}

    </div>			
            </div>			
            
{% endblock %}
